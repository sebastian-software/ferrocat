# ferrocat-linux-arm64-gnu

Native Linux arm64 glibc package for `ferrocat`.

This package contains the prebuilt `ferrocat.node` binary for Linux arm64 systems using glibc and
is installed as an optional dependency of `ferrocat`.
