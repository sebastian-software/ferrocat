packages:
  - "."

allowBuilds:
  esbuild: true
  unrs-resolver: true

# First-party packages — no supply-chain cooldown for our own releases.
minimumReleaseAgeExclude:
  - "@sebastian-gmbh/*"
  - "@sebastian-software/*"
  - vite@8.0.16
